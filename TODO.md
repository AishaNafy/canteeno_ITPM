# TODO: Search Bar + Dashboard + Download Receipt

## Plan
1. **Add Dependencies** (`pubspec.yaml`) — path_provider, share_plus, csv
2. **Home Screen Search + Dashboard** (`lib/screens/home_screen.dart`) — add search bar filtering today's specials, enhance info cards to proper dashboard
3. **Payment History Receipt Download** (`lib/screens/profile_sub_screens.dart`) — make stateful, add download button, expand mock data
4. **Real File Download Service** (`lib/utils/download_service.dart`) — generate real CSV files, use path_provider + share_plus
5. **Run `flutter pub get`** and verify build

## Progress
- [x] Step 1: Update pubspec.yaml
- [x] Step 2: Update home_screen.dart
- [x] Step 3: Update profile_sub_screens.dart (PaymentHistoryScreen)
- [x] Step 4: Update download_service.dart
- [x] Step 5: flutter pub get & verify

